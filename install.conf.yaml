- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.weechat: 
      path: weechat
      force: true
  #~/.dotfiles: ''
  # ~/.vimrc: vimrc
  # ~/.config/awesome: awesome/


- shell:
  - [git submodule update --init --recursive, Installing submodules]
