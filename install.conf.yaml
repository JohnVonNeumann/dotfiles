- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.weechat: 
      path: weechat
      force: true
  # ~/.vimrc: vimrc
    ~/.config/awesome:
      path: awesome
      force: true


- shell:
  - [git submodule update --init --recursive, Installing submodules]
